#ifndef MAIN_H
#define MAIN_H

#include <stddef.h>  /* for the size_t definition, if needed */

/* Function prototypes */

char *_memset(char *s, char b, unsigned int n);
int _putchar(char c);

#endif /* MAIN_H */
